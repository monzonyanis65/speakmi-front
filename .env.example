# Dirección de la API.
# Déjalo vacío para usar el proxy del servidor de desarrollo, que es lo recomendado:
# así el navegador ve un único origen, igual que en producción.
VITE_API_URL=

# Solo si quieres apuntar el proxy a otro sitio, por ejemplo la API ya desplegada.
VITE_API_PROXY=http://localhost:4000
